We are solving the problem usign MiniZinc for python. 
As specified in the official documentation, MiniZinc is expected to be in its default location, otherwise the environmental variable should be set.

We encountered some problems using the latest python version for this reason we had to roll back to the 3.6 interpreter version.

The python modules required are: matplotlib,minizinc